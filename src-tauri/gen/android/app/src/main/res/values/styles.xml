<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- This theme is applied to the Activity via AndroidManifest.xml.
         Using the DayNight NoActionBar parent prevents the default light
         gray window background that caused the gray line at the top. -->
    <style name="Theme.msic" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:windowBackground">@color/black</item>
        <item name="android:statusBarColor">@color/black</item>
        <item name="android:navigationBarColor">@color/black</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowLightNavigationBar">false</item>
    </style>
</resources>
